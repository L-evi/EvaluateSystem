<?xml version="1.0" encoding="UTF-8"?>
<!-- 用户User所用数据库语句文件 -->
<!-- 参考链接：http://c.biancheng.net/mybatis/mapper.html -->
<!-- 记得提供扫描入口，参考链接：https://blog.csdn.net/weixin_42109200/article/details/117412010 -->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.evaluate.dao.SyslogDao">
    <insert id="insertSyslog" useGeneratedKeys="true">
        INSERT INTO syslog(module, action, conditions, result, status, logTime, operator)
        VALUES (#{module}, #{action}, #{conditions}, #{result}, #{status}, #{logTime}, #{operator})
    </insert>
    <delete id="deletePageSyslog">
        DELETE
        FROM syslog
        WHERE ID IN
        <foreach collection="ids" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>
    <delete id="deleteSyslogByID">
        DELETE
        FROM syslog
        WHERE ID = #{ID}
    </delete>
    <select id="selectByID" resultType="com.project.evaluate.entity.Syslog">
        SELECT ID,
               module,
               action,
               conditions,
               result,
               status,
               logTime,
               operator,
               IP
        FROM syslog
        WHERE ID = #{ID}
    </select>
    <select id="selectPageSysLog" resultType="com.project.evaluate.entity.Syslog">
        SELECT ID, module, action, conditions, result, status, logTime, operator,IP
        FROM syslog
        <where>
            <if test="syslog.module != null and syslog.module !=''">
                module = #{syslog.module}
            </if>
            <if test="syslog.action != null and syslog.action != ''">
                AND `action` = #{syslog.action}
            </if>
            <if test="syslog.status != null and syslog.status !=''">
                AND `status` = #{syslog.status}
            </if>
            <if test="beforeTime != null and afterTime != null">
                AND logTime BETWEEN beforeTime and afterTime
            </if>
            <if test="syslog.operator != null and syslog.operator != ''">
                AND operator = #{syslog.operator}
            </if>
            <if test="syslog.IP != null and syslog.IP != ''">
                AND IP = #{IP}
            </if>
        </where>
    </select>
</mapper>