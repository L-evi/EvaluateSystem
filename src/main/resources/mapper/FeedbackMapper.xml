<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.evaluate.dao.FeedbackDao">
    <insert id="insert" useGeneratedKeys="true">
        INSERT INTO feedbackDO(title, content, feedbackTime, userID)
        VALUES (#{title}, #{content}, #{feedBackTime}, #{userID})
    </insert>

    <delete id="delete">
        DELETE
        FROM feedbackDO
        WHERE ID = #{ID}
    </delete>

    <select id="selectByID"
            resultType="com.project.evaluate.entity.DO.FeedbackDO">
        SELECT ID, title, content, feedbacktime, userID
        FROM feedbackDO
        WHERE ID = #{ID}
    </select>


    <select id="selectByFeedback"
            resultType="com.project.evaluate.entity.DO.FeedbackDO"
            parameterType="com.project.evaluate.entity.DO.FeedbackDO">
        SELECT ID, title, content, feedbackTime, userID
        FROM feedbackDO
        <where>
            <if test="ID != null and ID != 0">
                ID = #{ID}
            </if>
            <if test="title != null and title != ''">
                AND content = #{content}
            </if>
            <if test="userID != null and userID !=''">
                AND userID = #{userID}
            </if>
        </where>
    </select>


</mapper>