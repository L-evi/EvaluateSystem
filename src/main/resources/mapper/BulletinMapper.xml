<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.evaluate.dao.BulletinDao">
    <insert id="insertBulletin"
            useGeneratedKeys="true"
            parameterType="com.project.evaluate.entity.Bulletin">
        INSERT INTO bulletin(ID, subject, content, createTime, issueTime, expiredTime, operator)
        VALUES (#{ID}, #{subject}, #{content}, #{createTime}, #{issueTime}, #{expiredTime}, #{operator})
    </insert>
    <update id="updateBulletin">
        UPDATE bulletin

        <set>
            <if test="subject != null and subject != ''">
                subject = #{subject},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                createTime=#{createTime},
            </if>
            <if test="issueTime != null">
                issueTime=#{issueTime},
            </if>
            <if test="expiredTime != null">
                expiredTime = #{expiredTime},
            </if>
            <if test="operator != null and operator != ''">
                operator=#{operator}
            </if>
        </set>
        WHERE ID = #{ID}
    </update>
    <delete id="deleteByID">
        DELETE
        FROM bulletin
        WHERE ID = #{ID}
    </delete>

    <select id="selectByID" resultType="com.project.evaluate.entity.Bulletin">
        SELECT ID, subject, content, createTime, issueTime, expiredTime, operator
        FROM bulletin
        WHERE ID = #{ID}
    </select>
    <select id="selectByBulletin"
            resultType="com.project.evaluate.entity.Bulletin"
            parameterType="com.project.evaluate.entity.Bulletin">
        SELECT ID, `subject`, content, createTime, issueTime, expiredTime, operator
        FROM bulletin
        <where>
            <if test="bulletin.ID != null and bulletin.ID != 0">
                ID = #{bulletin.ID}
            </if>
            <if test="bulletin.subject != null and bulletin.subject != ''">
                AND `subject` = #{bulletin.subject}
            </if>
            <if test="bulletin.content != null and bulletin.content != ''">
                AND content = #{bulletin.content}
            </if>
            <if test="bulletin.operator != null and bulletin.operator != ''">
                AND operator != #{bulletin.operator}
            </if>
            <if test="date != null">
                AND #{date} BETWEEN issueTime AND expiredTime
            </if>
        </where>
    </select>
</mapper>