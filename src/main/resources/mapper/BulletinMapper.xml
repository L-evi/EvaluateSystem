<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.evaluate.dao.BulletinDao">
    <insert id="insertBulletin"
            useGeneratedKeys="true"
            parameterType="com.project.evaluate.entity.DO.BulletinDO">
        INSERT INTO bulletinDO(ID, subject, content, createTime, issueTime, expiredTime, operator)
        VALUES (#{ID}, #{subject}, #{content}, #{createTime}, #{issueTime}, #{expiredTime}, #{operator})
    </insert>
    <update id="updateBulletin">
        UPDATE bulletinDO

        <set>
            <if test="subject != null and subject != ''">
                subject = #{subject},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                createTime=#{createTime},
            </if>
            <if test="issueTime != null">
                issueTime=#{issueTime},
            </if>
            <if test="expiredTime != null">
                expiredTime = #{expiredTime},
            </if>
            <if test="operator != null and operator != ''">
                operator=#{operator}
            </if>
        </set>
        WHERE ID = #{ID}
    </update>
    <delete id="deleteByID">
        DELETE
        FROM bulletinDO
        WHERE ID = #{ID}
    </delete>

    <select id="selectByID" resultType="com.project.evaluate.entity.DO.BulletinDO">
        SELECT ID, subject, content, createTime, issueTime, expiredTime, operator
        FROM bulletinDO
        WHERE ID = #{ID}
    </select>
    <select id="selectByBulletin"
            resultType="com.project.evaluate.entity.DO.BulletinDO"
            parameterType="com.project.evaluate.entity.DO.BulletinDO">
        SELECT ID, subject, content, createTime, issueTime, expiredTime, operator
        FROM bulletinDO
        <where>
            <if test="bulletinDO.ID != null and bulletinDO.ID != 0">
                ID = #{bulletinDO.ID}
            </if>
            <if test="bulletinDO.subject != null and bulletinDO.subject != ''">
                AND subject = #{bulletinDO.subject}
            </if>
            <if test="bulletinDO.content != null and bulletinDO.content != ''">
                AND content = #{bulletinDO.content}
            </if>
            <if test="bulletinDO.operator != null and bulletinDO.operator != ''">
                AND operator != #{bulletinDO.operator}
            </if>
            <if test="date != null">
                AND #{date} BETWEEN issueTime AND expiredTime
            </if>
        </where>
    </select>
</mapper>